# Changelog

All notable changes to this package are documented here. Format based on
[Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

## [Unreleased]

### Added
- Initial package: `WeatherClient` implementing the `WeatherProvider`
  interface against the v1 API contract (`/v1/geocode`, `/v1/forecast`).
