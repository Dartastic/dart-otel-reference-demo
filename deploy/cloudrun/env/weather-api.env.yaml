# deploy/cloudrun/env/weather-api.env.yaml
#
# Static environment variables for the weather-api Cloud Run
# deployment. Dynamic values (cache-service URL, OTLP endpoint, etc.)
# are layered on by the deploy script via `--update-env-vars`.
#
# See the comments in cache-service.env.yaml for the rationale on
# what's set here vs auto-detected vs intentionally omitted.

OTEL_DEMO_MODE: "false"
OTEL_RESOURCE_ATTRIBUTES: "deployment.environment=cloudrun"
