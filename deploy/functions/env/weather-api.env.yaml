# deploy/functions/env/weather-api.env.yaml
#
# Static environment variables for the weather-api Cloud Functions
# Gen 2 deployment. See cache-service.env.yaml for the rationale on
# what's set here vs auto-detected vs intentionally omitted.

OTEL_DEMO_MODE: "false"
OTEL_RESOURCE_ATTRIBUTES: "deployment.environment=cloudfunctions,cloud.platform=gcp_cloud_functions,faas.name=weather-api-fn"
