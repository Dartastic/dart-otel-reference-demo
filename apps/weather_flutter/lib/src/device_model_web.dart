// Web stub for the conditional import in resources.dart.

/// Always null on web: there is no device model to report, and a browser
/// user-agent string is not what `device.model.identifier` means.
Future<String?> deviceModelIdentifier() async => null;
